#include "MotorControl.h"  // Include the motor control logic

void setup() {
  // Initialize the motors, servo, and perform the wrist movements
  setupMotorControl();  // Directly call the function defined in MotorControl.cpp
}

void loop() {
  // Empty loop, since all the actions occur in the setup function.
}
